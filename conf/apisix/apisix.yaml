routes:
  - uri: /*
    plugins:
      proxy-rewrite:
        host: "www.venkar.com"   # 强制把 Host 头改写为这个
    upstream:
      type: roundrobin
      scheme: https
      nodes:
        "web.venkar.com:443": 1
#END