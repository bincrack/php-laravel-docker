routes:
  - uri: /*
    plugins:
      proxy-rewrite:
        host: "www.baidu.com"   # 强制把 Host 头改写为这个
    upstream:
      type: roundrobin
      scheme: https
      nodes:
        "www.baidu.com:443": 1
#END